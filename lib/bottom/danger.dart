import 'package:flutter/material.dart';

class Dangers extends StatelessWidget {
  const Dangers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}